import styled from "styled-components";

const SGlobalWrapper = styled.div`
    max-width: 100%;
    width: 100%;
    height: calc(100vh - 64px);
    background-color: #f4f5f6;
`;

export default SGlobalWrapper;
